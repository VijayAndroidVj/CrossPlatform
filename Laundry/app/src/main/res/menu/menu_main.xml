<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <!--<item-->
    <!--android:id="@+id/add" android:icon="@android:drawable/ic_menu_add" app:showAsAction="always"   android:title="Add Service"/>-->
    <!--<item-->
    <!--android:id="@+id/reset" android:icon="@android:drawable/ic_menu_revert" app:showAsAction="always|withText" android:title="Add Item"/>-->

    <!-- <item
         android:id="@+id/add" android:icon="@android:drawable/ic_dialog_info" app:showAsAction="never" android:title="Add Service">
     </item>-->

    <item
        android:id="@+id/reset"
        android:icon="@android:drawable/ic_dialog_info"
        android:title="Settings"
        app:showAsAction="never"></item>
    <item
        android:id="@+id/about"
        android:icon="@android:drawable/ic_dialog_info"
        android:title="About Us"
        app:showAsAction="never"></item>
    <item
        android:id="@+id/exit"
        android:title="Logout"
        app:showAsAction="never"></item>
</menu>